{
  "name": "packup",
  "module": "src/packup.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun build . --compile --outfile packup-bin",
    "prestart": "bun run build",
    "start": "packup-bin"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@iarna/toml": "^2.2.5"
  }
}
